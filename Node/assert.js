const assert = require("assert");
/* The assert module provides a way of testing expressions. 
 If the expression evaluates to 0, or false, an assertion 
failure is being caused, and the program is
 terminated.*/
//assert(expression, message);
//The assert() method tests if a given expression is true or not.

// assert(5 < 7, "This is false");

//assert.deepEqual() method tests if two objects,
// assert.deepEqual(2,4,'compare it');

//The assert.ok() method tests if a given expression is true or not.
// assert.ok(4>5,"Hi");